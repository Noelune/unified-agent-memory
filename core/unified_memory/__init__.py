"""unified-agent-memory core — shared memory system for multiple AI agents.

A single Obsidian vault is the source of truth; every agent reads canonical
notes, writes facts to the submission inbox, and a promoter promotes them.

This package is intentionally dependency-free (Python standard library only).
"""
__version__ = "0.3.0"
